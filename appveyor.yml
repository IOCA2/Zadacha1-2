image: Ubuntu2004
stack: jdk 11
branches:
  only:
    - main
build: off
install:
  - test -f app-mbank.jar && java -jar app-mbank.jar & || exit 1
  - chmod +x gradlew
build_script:
  - ./gradlew test --info